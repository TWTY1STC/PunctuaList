<tr>
  <td>
    <%= item.name %>
  </td>
  <td>
    <%= time_ago_in_words(item.created_at) %>
  </td>
  <td>
    <%= link_to "", user_item_path(current_user, item.id), method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
  </td>
</tr>
